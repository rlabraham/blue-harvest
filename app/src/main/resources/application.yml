spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://localhost:3306/blueharvest
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:blueharvest}


server:
  port: 8080
